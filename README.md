# BULK-USER-METRIC-AGGREGATOR
R → data processing data.frame → user data lapply / sapply → vectorized-style mapping sum / mean → aggregation subset → active users ggplot2 → charts Shiny → Admin Dashboard
